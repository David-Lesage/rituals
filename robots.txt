User-agent: *
Allow: /

# /solune et /au-nid ont ete SUPPRIMEES le 17/08/2026. Ce sont maintenant des
# redirections 301 (voir vercel.json) vers /e-motion et /le-nid.
# ⚠️ Surtout PAS de Disallow ici : il empecherait Google de voir la redirection,
# donc de reporter le referencement sur la page qui remplace.

# Adresse stable de connexion des beta-testeurs de Guso Facile. Ce n'est pas une
# page : c'est une redirection posee dans vercel.json vers l'application. Elle
# n'a aucune valeur en referencement et n'a rien a faire dans Google.
Disallow: /guso-facile/app
Disallow: /guso-facile/invitation

# /David-Lesage-Lucie-Andersen A ETE OUVERTE AU PUBLIC le 30/08/2026, sur
# decision de David : elle est entree dans le menu (« Sur scene »), dans le
# plan du site et dans verif_site.PAGES, et sa balise « noindex » a ete
# retiree. Son `Disallow` a donc disparu d'ici : le laisser aurait donne une
# page listee au menu que Google aurait refuse d'explorer.
#
# Seule son ANCIENNE adresse reste interdite. Ce n'est plus une page : c'est
# un renvoi vers la nouvelle, pour qu'un lien envoye avant le changement de
# nom ne tombe pas en 404. Elle n'a aucun referencement a transferer — elle a
# vecu en « noindex » du premier au dernier jour — et n'a donc rien a faire
# dans Google.
Disallow: /David-Lesage-Lucie-Electric-Violoniste

Sitemap: https://www.resonancesproductions.org/sitemap.xml
