User-agent: *
Allow: /

# /solune et /au-nid ont ete SUPPRIMEES le 17/08/2026. Ce sont maintenant des
# redirections 301 (voir vercel.json) vers /e-motion et /le-nid.
# ⚠️ Surtout PAS de Disallow ici : il empecherait Google de voir la redirection,
# donc de reporter le referencement sur la page qui remplace.

# Adresse stable de connexion des beta-testeurs de Guso Facile. Ce n'est pas une
# page : c'est une redirection posee dans vercel.json vers l'application. Elle
# n'a aucune valeur en referencement et n'a rien a faire dans Google.
Disallow: /guso-facile/app
Disallow: /guso-facile/invitation

# Page de promo du duo David Lesage & Lucie, creee le 30/08/2026. Elle est
# VOLONTAIREMENT invisible : elle n'est dans aucun menu, elle n'est pas dans
# le plan du site, et personne n'a a la trouver par Google — seules les
# agences a qui David envoie l'adresse doivent y arriver. Ce n'est pas une
# page supprimee : il n'y a rien a rediriger, et donc aucune raison de la
# laisser explorer (voir la note sur /solune plus haut, qui est le cas
# INVERSE). La page porte en plus une balise « noindex » : robots.txt empeche
# l'exploration, mais pas l'indexation d'une adresse apprise ailleurs.
Disallow: /David-Lesage-Lucie-Andersen
# L'adresse a change le 30/08/2026 (le nom de Lucie a ete confirme). L'ancienne
# reste interdite elle aussi : elle sert encore de page de renvoi vers la
# nouvelle, pour qu'un lien deja envoye ne tombe pas en 404. Contrairement au
# cas /solune plus haut, il n'y a ici AUCUN referencement a transferer — la
# page est en « noindex » depuis le premier jour et n'a jamais ete indexee.
Disallow: /David-Lesage-Lucie-Electric-Violoniste

Sitemap: https://www.resonancesproductions.org/sitemap.xml
